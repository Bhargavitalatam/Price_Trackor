version: '3.8'

services:
  price-trackor:
    container_name: price-trackor-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
